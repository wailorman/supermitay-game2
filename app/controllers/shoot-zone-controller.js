define( [
        'app'
    ],

    function ( app ) {

        "use strict";

        return app.controller( 'ShootZoneCtrl', [ '$scope', function( $scope ){
            this.text = '123';


        } ] );

    }
);